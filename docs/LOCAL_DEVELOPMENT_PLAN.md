# Local development plan

Status: local-only prototype started. No external publication, marketplace action, app registration, or outreach performed.

## Next build increments

1. Add policy file support for required files, ignored rules, and severity overrides.
2. Add fixtures for healthy repos to control false positives.
3. Add GitHub Action usage snippet writing Markdown to `$GITHUB_STEP_SUMMARY` and JSON to artifacts.
4. Reuse deprecation rule inventory from `github-actions-deprecation-preflight` or share a common rules file.
5. Draft public README and sample reports for approval review.

## Approval gates

Approval is required before GitHub push/release, package registry publishing, GitHub Marketplace/Action listing, outreach, or payment/Sponsors/storefront setup.
